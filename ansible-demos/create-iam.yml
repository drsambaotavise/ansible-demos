# creating IAM policy code

---
- name: Create basic IAM policy
  iam_policy:
    name: my-policy
    state: present
    policy_document: |
      {
        "Version": "2012-10-17",
        "Statement": [
          {
            "Effect": "Allow",
            "Action": [
              "s3:GetObject"
            ],
            "Resource": [
              "arn:aws:s3:::example-bucket/*"
            ]
          }
        ]
      }

React

Reply










